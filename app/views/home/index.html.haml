%h1 Home#index
%p Find me in app/views/home/index.html.haml

- @slides.each do |slide|
  .well.slide-list
    %a(href="/streaming/#{slide["ID"]}")
    .span
      =image_tag "#{slide["ThumbnailURL"]}"
    %p= slide["Title"]

=link_to "sign_out",  destroy_user_session_path,  method: :delete
