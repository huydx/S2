= form_tag question_add_answer_path, remote: true do
  = hidden_field_tag 'question-id', @question_id
  = text_area_tag 'answer-content', @answer_content
  = button_tag "Submit", data: {"disable_width" => "Please wait.."}, class: "btn btn-primary btn-lg btn-block"
